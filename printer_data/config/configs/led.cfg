





#####################################################################
# 	LED Control
#####################################################################

[output_pin caselight]
# Chamber Lighting - HE2 Connector (Optional)
pin: PC12
pwm:true
shutdown_value: 0
value: 0.20
cycle_time: 0.008333

[neopixel skirt]
pin: PA9
#   The pin connected to the neopixel. This parameter must be
#   provided.
chain_count: 20
#   The number of Neopixel chips that are "daisy chained" to the
#   provided pin. The default is 1 (which indicates only a single
#   Neopixel is connected to the pin).
color_order: GRB
#   Set the pixel order required by the LED hardware (using a string
#   containing the letters R, G, B, W with W optional). Alternatively,
#   this may be a comma separated list of pixel orders - one for each
#   LED in the chain. The default is GRB.
initial_RED: 0.1
initial_GREEN: 0.0
initial_BLUE: 0.0
#initial_WHITE: 0.0
#   See the "led" section for information on these parameters.

[gcode_macro Skirt_LED_AUS]
gcode:
    SET_LED LED=skirt RED=0 GREEN=0 BLUE=0

[gcode_macro Skirt_LED_ROT]
gcode:
    SET_LED LED=skirt RED=1 GREEN=0 BLUE=0

#[led_effect skirt_heating]
#autostart:              false
#frame_rate:             24
#leds:
#    neopixel:skirt
#layers:
#    breathing  10 1 top (1,0,0)

#[led_effect skirt_progress_bar]
#leds:
#    neopixel:skirt
#autostart:                          false
#frame_rate:                         24
#layers:
#    progress   1  0 add         ( 0, 0,   1),( 0, 0.1, 0.6)
#    static     0  0 top         ( 0, 0, 0.1)